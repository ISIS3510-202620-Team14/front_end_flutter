class SubjectProgress {
  final String title;
  final int percent;
  final int evaluated;
  final int total;

  const SubjectProgress({
    required this.title,
    required this.percent,
    required this.evaluated,
    required this.total,
  });
}
